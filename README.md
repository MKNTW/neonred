# NEON RED - Vue 3 версия

## 📋 Описание

Этот проект представляет собой переписанную версию интернет-магазина NEON RED с использованием Vue 3 и Composition API. Все прямые манипуляции DOM заменены на реактивные данные Vue, код разделён на логические компоненты и композаблы.

## 🚀 Структура проекта

```
neon-red-main/
├── src/
│   ├── components/          # Vue компоненты
│   │   ├── AdminModal.vue
│   │   ├── AgeVerification.vue
│   │   ├── AuthModal.vue
│   │   ├── CartModal.vue
│   │   ├── ConfirmDialog.vue
│   │   ├── ConnectionStatus.vue
│   │   ├── Header.vue
│   │   ├── InputDialog.vue
│   │   ├── LoginForm.vue
│   │   ├── Modal.vue
│   │   ├── OrderDetailsModal.vue
│   │   ├── ProductCard.vue
│   │   ├── ProductEditModal.vue
│   │   ├── ProductList.vue
│   │   ├── ProductPagination.vue
│   │   ├── ProfileField.vue
│   │   ├── ProfileFieldEdit.vue
│   │   ├── ProfileModal.vue
│   │   ├── RegisterForm.vue
│   │   ├── StepIndicator.vue
│   │   └── ToastContainer.vue
│   ├── composables/         # Композаблы (переиспользуемая логика)
│   │   ├── useApi.js        # Работа с API
│   │   ├── useAuth.js       # Аутентификация
│   │   ├── useCart.js       # Корзина
│   │   ├── useOptimization.js # Оптимизация производительности
│   │   ├── useProducts.js   # Товары
│   │   └── useToast.js      # Уведомления
│   ├── styles/
│   │   └── main.css         # Глобальные стили
│   ├── App.vue              # Главный компонент
│   └── main.js              # Точка входа
├── index.html                # HTML шаблон
├── server.cjs                # Backend сервер (Express)
├── vite.config.js           # Конфигурация Vite
└── package.json             # Зависимости
```

## ✨ Ключевые улучшения

### 1. Архитектура

- **Разделение на компоненты**: Каждая функциональная область вынесена в отдельный компонент
- **Композаблы**: Переиспользуемая логика вынесена в композаблы (`useAuth`, `useCart`, `useProducts`, `useApi`, `useToast`)
- **Реактивность**: Все данные реактивны через `ref` и `reactive`
- **Отсутствие прямых манипуляций DOM**: Вместо `document.querySelector` используется реактивная система Vue

### 2. Функциональность

- ✅ Аутентификация (логин, регистрация с подтверждением email)
- ✅ Управление корзиной с синхронизацией с сервером
- ✅ Каталог товаров с пагинацией
- ✅ Профиль пользователя с редактированием
- ✅ Админ-панель (управление товарами, пользователями, заказами)
- ✅ Оформление заказов
- ✅ Toast уведомления
- ✅ Модальные окна
- ✅ Адаптивный дизайн

## 🛠️ Установка и запуск

### Требования

- Node.js 18+ 
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Запуск разработки

**Frontend (Vue 3):**
```bash
npm run dev
```
Приложение будет доступно по адресу: `http://localhost:3000`

**Backend (Express):**
```bash
npm run server:dev
```
Сервер будет доступен по адресу: `http://localhost:3001`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Backend
PORT=3001
JWT_SECRET=your-secret-key-here
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-key
RESEND_API_KEY=your-resend-api-key

# Frontend (для production)
VITE_API_URL=https://your-backend-service.onrender.com/api
```

**Важно для разработки:**
- Если `VITE_API_URL` не указан, frontend автоматически использует `http://localhost:3001/api`
- Для production обязательно укажите `VITE_API_URL` с URL вашего backend сервера

## 🚀 Деплой

### Структура деплоя

- **Frontend (Vue 3)**: Vercel
- **Backend (Node.js)**: Render.com

### Настройка Vercel (Frontend)

#### 1. Переменные окружения

В настройках проекта Vercel добавьте переменную окружения:

```
VITE_API_URL=https://your-backend-service.onrender.com/api
```

**Важно**: Замените `your-backend-service.onrender.com` на реальный URL вашего backend сервиса на Render.com

#### 2. Build Settings

Vercel автоматически определит настройки для Vite, но убедитесь что:

- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

#### 3. Деплой

1. Подключите ваш GitHub репозиторий к Vercel
2. Укажите переменную окружения `VITE_API_URL`
3. Нажмите "Deploy"

### Настройка Render.com (Backend)

#### 1. Создание сервиса

1. Зайдите на [Render.com](https://render.com)
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий

#### 2. Настройки сервиса

- **Name**: `neon-red-backend` (или любое другое имя)
- **Environment**: `Node`
- **Build Command**: `npm install` (или оставьте пустым)
- **Start Command**: `npm start` или `node server.cjs`
- **Plan**: Выберите подходящий план (Free tier доступен)

#### 3. Переменные окружения

Добавьте следующие переменные окружения в настройках сервиса:

```
NODE_ENV=production
PORT=10000
JWT_SECRET=your-very-secure-secret-key-here
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-key
RESEND_API_KEY=your-resend-api-key
FRONTEND_URL=https://your-vercel-app.vercel.app
```

**Важно:**
- `PORT` на Render.com обычно `10000` (или используйте переменную `PORT` из окружения)
- `FRONTEND_URL` нужен для CORS настроек

#### 4. Деплой

После настройки нажмите "Create Web Service". Render.com автоматически:
- Установит зависимости
- Запустит сервер
- Предоставит URL (например: `https://neon-red-backend.onrender.com`)

#### 5. Обновление CORS

После получения URL от Render.com, обновите `FRONTEND_URL` в переменных окружения, если нужно.

### Проверка деплоя

1. **Frontend**: Откройте ваш Vercel URL
2. **Backend**: Проверьте доступность API: `https://your-backend.onrender.com/api/status`
3. Убедитесь, что `VITE_API_URL` в Vercel указывает на правильный backend URL

## 📝 API Endpoints

### Публичные

- `GET /api/products` - Список товаров (с пагинацией)
- `GET /api/products/:id` - Получить товар по ID
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `GET /api/check-username/:username` - Проверка доступности username
- `POST /api/verify-email` - Подтверждение email
- `GET /api/status` - Проверка статуса сервера

### Защищенные (требуют токен)

- `GET /api/validate-token` - Валидация токена
- `GET /api/profile` - Получить профиль
- `PUT /api/profile` - Обновить профиль
- `DELETE /api/profile` - Удалить аккаунт
- `POST /api/profile/avatar` - Загрузить аватар
- `GET /api/orders` - Список заказов пользователя
- `POST /api/orders` - Создать заказ

### Админ (требуют токен + права администратора)

- `GET /api/admin/products` - Список всех товаров
- `POST /api/admin/products` - Создать товар
- `PUT /api/admin/products/:id` - Обновить товар
- `DELETE /api/admin/products/:id` - Удалить товар
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/orders` - Список всех заказов
- `PUT /api/admin/orders/:id/status` - Изменить статус заказа

## 🎨 Особенности

- **Киберпанк дизайн**: Neon эффекты, градиенты, анимации
- **Адаптивность**: Работает на всех устройствах
- **Производительность**: Lazy loading изображений, оптимизация рендеринга
- **Доступность**: ARIA атрибуты, keyboard navigation
- **UX**: Toast уведомления, loading состояния, обработка ошибок

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей (bcrypt)
- Валидация данных на клиенте и сервере
- CORS настройки
- Защита от XSS

## 📦 Зависимости

### Frontend
- Vue 3.4+
- Vite 5+

### Backend
- Express 4.18+
- Supabase (база данных)
- JWT (аутентификация)
- bcrypt (хеширование паролей)
- Resend (отправка email)

## 🤝 Разработка

### Структура композаблов

- `useApi` - Работа с API, обработка ошибок, retry логика
- `useAuth` - Аутентификация, управление токенами
- `useCart` - Корзина, синхронизация с сервером
- `useProducts` - Загрузка и управление товарами
- `useToast` - Система уведомлений
- `useOptimization` - Оптимизация производительности

### Компоненты

Все компоненты используют Composition API (`<script setup>`) и scoped стили.

## 📄 Лицензия

MIT

## 👤 Автор

NEON RED Team

