{
  "name": "neon-red-backend",
  "version": "1.1.0",
  "description": "Backend API для NEON RED магазина с поддержкой мобильных устройств",
  "version": "1.2.0",
  "description": "Backend API для NEON RED магазина с поддержкой мобильных устройств и админ-панелью",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "mobile-test": "PORT=3002 node server.js"
    "setup-db": "node -e \"require('fs').readFile('admin_supabase.sql', 'utf8', (err, data) => console.log(data))\"",
    "deploy": "npm run setup-db && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "@supabase/supabase-js": "^2.39.3",
    "dotenv": "^16.3.1",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "browserslist": [
    "last 2 versions",
    "> 1%",
    "iOS >= 12",
    "Android >= 6"
  ]
}
